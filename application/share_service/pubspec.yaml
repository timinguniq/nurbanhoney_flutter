name: share_service
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.5

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.0.2
  share_repository:
    path: ../../data/share_repository

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
