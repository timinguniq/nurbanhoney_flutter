import 'package:flutter/material.dart';

class NurbanRankTabView extends StatelessWidget {
  const NurbanRankTabView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('NurbanRankTabView'),
    );
  }
}
