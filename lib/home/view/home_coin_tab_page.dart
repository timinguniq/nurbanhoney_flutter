import 'package:flutter/material.dart';

class HomeCoinTabPage extends StatelessWidget {
  const HomeCoinTabPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('HomeCoinTabPage'),
    );
  }
}
